/**
 * @NApiVersion 2.x
 * @NScriptType Suitelet
 * @NModuleScope SameAccount
 */
define(["N/search"], function (search, serverWidget) {
  function onRequest(context) {}

  return {
    onRequest: onRequest,
  };
});
