/**
 *@NApiVersion 2.x
 *@NScriptType ScheduledScript
 */
define(['N/runtime', 'N/search', 'N/record', 'N/file'], function(runtime, search, record, file) {

    function execute(context) {
        
    }

    return {
        execute: execute
    }
});
